#pedir a altura e largura:
altura = float(input('digite a altura do retangulo: '))
largura = float(input('digite a largura do retangulo: '))
#calculo da area:
area = altura * largura 
print(f'a area do retangulo é: {area}')